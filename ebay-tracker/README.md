# 🔍 eBay Tracker

Автоматический мониторинг новых лотов на eBay по ключевым словам с уведомлениями в Telegram.

## 📋 Возможности

- ✅ **Мониторинг по ключевым словам** - отслеживайте несколько поисковых запросов одновременно
- ✅ **eBay Browse API** - современный API с сортировкой по новизне
- ✅ **Fallback на Finding API** - резервный вариант, работает без OAuth
- ✅ **База данных SQLite** - хранение истории и предотвращение дубликатов
- ✅ **Telegram уведомления** - мгновенные оповещения о новых лотах (опционально)
- ✅ **Фильтры** - по цене, состоянию товара, региону
- ✅ **Статистика** - отслеживание найденных лотов
- ✅ **Автоматизация** - запуск через cron, GitHub Actions или вручную

## 🚀 Быстрый старт

### 1. Установка

**Linux / macOS:**

```bash
cd ebay-tracker

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

**Windows (PowerShell):**

```powershell
cd ebay-tracker

# Создайте виртуальное окружение
python -m venv venv

# Активируйте (PowerShell)
venv\Scripts\Activate.ps1

# Если ошибка ExecutionPolicy, выполните:
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Установите зависимости
pip install -r requirements.txt
```

**Примечание для Windows:** Используйте `python` вместо `python3`. Если команда не найдена, установите Python с https://www.python.org/downloads/ (не забудьте отметить "Add Python to PATH").

### 2. Настройка eBay API

1. Перейдите на [developer.ebay.com](https://developer.ebay.com/my/keys)
2. Войдите или создайте аккаунт
3. Создайте **Application Keys** (Sandbox или Production)
4. Скопируйте:
   - **App ID (Client ID)**
   - **Cert ID (Client Secret)** (опционально, для Browse API)

### 3. Настройка Telegram (опционально)

Если хотите получать уведомления в Telegram:

1. Создайте бота через [@BotFather](https://t.me/BotFather):
   - Отправьте команду `/newbot`
   - Выберите имя и username
   - Получите **Bot Token**

2. Получите свой Chat ID:
   - Напишите что-нибудь боту
   - Откройте `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - Найдите `"chat":{"id":123456789}` в ответе

### 4. Конфигурация

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env
nano .env  # или любой редактор
```

**Минимальная конфигурация:**

```env
# eBay API (обязательно)
EBAY_APP_ID=your_app_id_here

# Поисковые запросы (обязательно)
SEARCH_KEYWORDS=joy division cassette,depeche mode tape

# Остальное опционально
```

**Полная конфигурация:**

```env
# eBay API
EBAY_APP_ID=your_app_id_here
EBAY_CERT_ID=your_cert_id_here  # Для Browse API (OAuth)

# Telegram (опционально)
TELEGRAM_BOT_TOKEN=123456:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789

# Настройки поиска
SEARCH_KEYWORDS=joy division cassette,new order tape,depeche mode cassette
EBAY_SITE_ID=EBAY_US  # EBAY_US, EBAY_UK, EBAY_DE, и т.д.
MAX_RESULTS=50

# Фильтры (опционально)
MIN_PRICE=10
MAX_PRICE=100
CONDITION_FILTER=NEW,LIKE_NEW,USED_EXCELLENT
```

### 5. Запуск

#### Локальный запуск

**Linux / macOS (через скрипт):**

```bash
./run.sh
```

**Windows или прямой запуск:**

```bash
# Активируйте виртуальное окружение если еще не активировано
# Linux/Mac: source venv/bin/activate
# Windows: venv\Scripts\Activate.ps1

# Запустите tracker
python tracker.py
```

#### ☁️ Запуск в облаке (рекомендуется)

**Хотите, чтобы трекер работал 24/7 без вашего компьютера?**

Используйте **GitHub Actions** - полностью бесплатно:

- ✅ Работает в облаке постоянно
- ✅ Запуск каждые 30 минут автоматически
- ✅ Не нужен включенный компьютер
- ✅ 2000 минут/месяц бесплатно (достаточно!)

**Настройка за 5 минут:** См. [GITHUB_ACTIONS.md](./GITHUB_ACTIONS.md)

Кратко:
1. Запушьте код на GitHub
2. Добавьте секреты в Settings → Secrets
3. Готово - трекер запустится автоматически

## 📖 Использование

### Разовый запуск

Просто запустите скрипт - он проверит все ключевые слова и выведет новые лоты:

```bash
./run.sh
```

**Вывод:**

```
🚀 eBay Tracker started
🔍 Keywords: joy division cassette, new order tape
📍 Site: EBAY_US
💬 Telegram: enabled
──────────────────────────────────────────────────

🔎 Searching for: joy division cassette
   Found 12 items

   ✨ NEW: Joy Division - Unknown Pleasures - Cassette Tape
      💰 24.99 USD
      🔗 https://ebay.com/itm/123456789

   ✅ 3 new items for 'joy division cassette'

──────────────────────────────────────────────────
✅ Scan complete: 5 new items total
📊 Database: 127 total items (5 today)
```

### Автоматический запуск (cron)

Добавьте в crontab для запуска каждые 30 минут:

```bash
# Открыть crontab
crontab -e

# Добавить строку (измените путь):
*/30 * * * * cd /path/to/rewplay/ebay-tracker && ./run.sh >> logs/tracker.log 2>&1
```

### Запуск через GitHub Actions

Создайте файл `.github/workflows/ebay-tracker.yml`:

```yaml
name: eBay Tracker

on:
  schedule:
    - cron: '*/30 * * * *'  # Каждые 30 минут
  workflow_dispatch:  # Ручной запуск

jobs:
  track:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          cd ebay-tracker
          pip install -r requirements.txt

      - name: Run tracker
        env:
          EBAY_APP_ID: ${{ secrets.EBAY_APP_ID }}
          EBAY_CERT_ID: ${{ secrets.EBAY_CERT_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SEARCH_KEYWORDS: ${{ secrets.SEARCH_KEYWORDS }}
        run: |
          cd ebay-tracker
          python tracker.py
```

Добавьте секреты в **Settings → Secrets → Actions**.

## ⚙️ Конфигурация

### Поддерживаемые регионы (EBAY_SITE_ID)

- `EBAY_US` - США (по умолчанию)
- `EBAY_UK` - Великобритания
- `EBAY_DE` - Германия
- `EBAY_AU` - Австралия
- `EBAY_CA` - Канада
- `EBAY_FR` - Франция
- `EBAY_IT` - Италия
- `EBAY_ES` - Испания
- `EBAY_AT` - Австрия

### Фильтры по состоянию (CONDITION_FILTER)

Значения через запятую:

- `NEW` - Новый
- `LIKE_NEW` - Как новый
- `USED_EXCELLENT` - Б/У отличное
- `USED_GOOD` - Б/У хорошее
- `USED_ACCEPTABLE` - Б/У приемлемое
- `FOR_PARTS_OR_NOT_WORKING` - На запчасти

Пример:

```env
CONDITION_FILTER=NEW,LIKE_NEW,USED_EXCELLENT
```

## 🔧 Структура проекта

```
ebay-tracker/
├── tracker.py          # Основной скрипт
├── config.py           # Управление конфигурацией
├── database.py         # SQLite база данных
├── notifier.py         # Telegram уведомления
├── requirements.txt    # Python зависимости
├── .env.example        # Шаблон конфигурации
├── .env               # Ваша конфигурация (не в git)
├── run.sh             # Скрипт быстрого запуска
├── db/                # База данных (создается автоматически)
│   └── tracker.db     # SQLite файл
└── README.md          # Документация
```

## 📊 База данных

Tracker использует SQLite для хранения информации о просмотренных лотах:

**Схема таблицы `seen_items`:**

- `item_id` - ID лота на eBay (первичный ключ)
- `title` - Название
- `price` - Цена
- `currency` - Валюта
- `url` - Ссылка
- `image_url` - URL изображения
- `condition_display` - Состояние
- `seller_username` - Продавец
- `listing_date` - Дата размещения
- `search_keyword` - По какому запросу найден
- `first_seen_at` - Когда впервые обнаружен
- `notified` - Отправлено ли уведомление

**Просмотр базы:**

```bash
sqlite3 db/tracker.db

# SQL запросы
SELECT COUNT(*) FROM seen_items;
SELECT * FROM seen_items ORDER BY first_seen_at DESC LIMIT 10;
SELECT search_keyword, COUNT(*) FROM seen_items GROUP BY search_keyword;
```

## 🤖 API eBay

### Browse API (рекомендуется)

**Преимущества:**
- ✅ Сортировка `sort=newlyListed`
- ✅ Современный REST API
- ✅ До 200 результатов за запрос
- ✅ Лучшая фильтрация

**Недостатки:**
- ❌ Требует OAuth 2.0 (App ID + Cert ID)
- ❌ Немного сложнее настройка

### Finding API (резервный)

**Преимущества:**
- ✅ Простая настройка (только App ID)
- ✅ Не требует OAuth
- ✅ Стабильный и надежный

**Недостатки:**
- ❌ Устаревший (legacy)
- ❌ До 100 результатов за запрос
- ❌ Менее точная сортировка

**Автоматический fallback:** Tracker пытается использовать Browse API, и если не удается - автоматически переключается на Finding API.

## 🔔 Уведомления

При настроенном Telegram вы получите:

**При новом лоте:**

```
🆕 Новый лот на eBay!

📦 Joy Division - Unknown Pleasures Cassette

💰 24.99 USD
📋 Состояние: Used - Excellent
👤 Продавец: vinyl_collector
📅 Размещено: 2024-11-08 14:30

🔍 Найдено по: joy division cassette

🔗 Открыть на eBay
```

**С изображением** (если доступно):

![Пример уведомления с картинкой]

## 📈 Статистика и обслуживание

### Просмотр статистики

Tracker выводит статистику после каждого запуска:

```python
from database import Database
from config import Config

db = Database(Config.DB_PATH)
stats = db.get_stats()

print(f"Всего лотов: {stats['total_items']}")
print(f"Сегодня: {stats['items_today']}")
print(f"По ключевым словам: {stats['items_by_keyword']}")
```

### Очистка старых записей

Удалить лоты старше 30 дней:

```python
from database import Database
from config import Config

db = Database(Config.DB_PATH)
deleted = db.cleanup_old_items(days=30)
print(f"Удалено {deleted} старых записей")
```

Или через SQLite:

```bash
sqlite3 db/tracker.db "DELETE FROM seen_items WHERE first_seen_at < datetime('now', '-30 days');"
```

## 🐛 Решение проблем

### "Configuration errors: EBAY_APP_ID is required"

Создайте `.env` файл из `.env.example` и заполните `EBAY_APP_ID`.

### "OAuth error: 401"

Проверьте:
- `EBAY_APP_ID` и `EBAY_CERT_ID` корректны
- Ключи взяты из **Production** (не Sandbox)
- Ключи не истекли

Tracker автоматически переключится на Finding API если OAuth не работает.

### "No new items" всегда

Возможные причины:
- Все лоты уже в базе данных
- Слишком специфичные ключевые слова
- Слишком строгие фильтры (цена, состояние)

Попробуйте:
- Удалить базу: `rm db/tracker.db`
- Расширить диапазон цен
- Убрать фильтры по состоянию

### Telegram не отправляет сообщения

Проверьте:
- `TELEGRAM_BOT_TOKEN` корректен
- `TELEGRAM_CHAT_ID` это **ваш** chat ID (не username!)
- Вы написали боту хотя бы одно сообщение

Получить Chat ID: `https://api.telegram.org/bot<TOKEN>/getUpdates`

## 💡 Советы и лучшие практики

### Оптимальная частота запуска

- **Каждые 30 минут** - оптимально для большинства случаев
- **Каждые 15 минут** - для популярных категорий
- **Каждый час** - для редких товаров

eBay имеет лимит **5000 запросов/день** на App ID.

### Эффективные ключевые слова

**Хорошо:**
```env
SEARCH_KEYWORDS=joy division cassette,new order tape france,depeche mode cassette sealed
```

**Плохо (слишком широко):**
```env
SEARCH_KEYWORDS=cassette,tape,music
```

**Используйте:**
- Конкретные названия альбомов/исполнителей
- Специфичные термины (sealed, rare, vintage)
- Комбинации с регионом/годом

### Мониторинг нескольких категорий

Создайте несколько инстансов с разными `.env` файлами:

```bash
ebay-tracker/
├── .env.cassettes     # Кассеты
├── .env.vinyl         # Винил
└── .env.rare-books    # Книги

# Запуск
python tracker.py --config .env.cassettes
python tracker.py --config .env.vinyl
```

### Резервное копирование базы

```bash
# Создать backup
cp db/tracker.db db/tracker.backup.db

# Автоматический backup через cron
0 0 * * * cp /path/to/ebay-tracker/db/tracker.db /path/to/backups/tracker-$(date +\%Y\%m\%d).db
```

## 🔮 Идеи для развития

- [ ] Web интерфейс для управления
- [ ] Email уведомления
- [ ] Slack/Discord интеграция
- [ ] Мобильное приложение
- [ ] Автоматическое создание watchlist на eBay
- [ ] Анализ цен и трендов
- [ ] Сохранение изображений локально
- [ ] Экспорт в CSV/Excel
- [ ] Интеграция с другими маркетплейсами (Etsy, Reverb)

## 📄 Лицензия

MIT License - используйте свободно для личных и коммерческих целей.

## 🤝 Поддержка

Нашли баг? Есть идея для улучшения? Создайте issue в репозитории.

---

**Создано с использованием:**
- 🛍️ [eBay API](https://developer.ebay.com) - Browse API и Finding API
- 🐍 [Python](https://python.org) - Основной язык
- 💬 [python-telegram-bot](https://python-telegram-bot.org) - Telegram уведомления
- 💾 [SQLite](https://sqlite.org) - База данных

**Готово к использованию!** Настройте API ключи и начинайте отслеживать лоты на eBay 🚀
