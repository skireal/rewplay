# 🚀 Запуск eBay Tracker в облаке через GitHub Actions

Настройка автоматического мониторинга eBay в облаке - **полностью бесплатно**.

## ✅ Преимущества

- **Бесплатно:** 2000 минут/месяц для приватных репозиториев, неограниченно для публичных
- **Автоматически:** Запуск каждые 30 минут без участия
- **Надежно:** Работает 24/7, даже если ваш компьютер выключен
- **База данных:** Автоматически сохраняется между запусками
- **Telegram:** Уведомления приходят на телефон

## 📋 Настройка за 5 минут

### Шаг 1: Запушьте код на GitHub

```bash
# Если репозиторий еще не на GitHub, создайте его:
# 1. Создайте новый репозиторий на github.com
# 2. Запушьте код:

git remote add origin https://github.com/ваш-username/rewplay.git
git branch -M main
git push -u origin main
```

### Шаг 2: Добавьте секреты в GitHub

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret** и добавьте:

**Обязательные секреты:**

| Имя секрета | Значение | Где взять |
|------------|----------|-----------|
| `EBAY_APP_ID` | Ваш eBay App ID | [developer.ebay.com/my/keys](https://developer.ebay.com/my/keys) |
| `SEARCH_KEYWORDS` | Ключевые слова через запятую | `joy division cassette,new order tape` |

**Опциональные секреты (для Telegram уведомлений):**

| Имя секрета | Значение | Где взять |
|------------|----------|-----------|
| `TELEGRAM_BOT_TOKEN` | Токен вашего бота | [@BotFather](https://t.me/BotFather) в Telegram |
| `TELEGRAM_CHAT_ID` | Ваш Chat ID | `https://api.telegram.org/bot<TOKEN>/getUpdates` |

**Дополнительные настройки (опционально):**

| Имя секрета | Значение | Пример |
|------------|----------|--------|
| `EBAY_CERT_ID` | Cert ID для OAuth | Для Browse API |
| `EBAY_SITE_ID` | Регион eBay | `EBAY_US`, `EBAY_UK`, `EBAY_DE` |
| `MAX_RESULTS` | Макс. результатов | `50` |
| `MIN_PRICE` | Мин. цена | `10` |
| `MAX_PRICE` | Макс. цена | `100` |
| `CONDITION_FILTER` | Фильтр состояния | `NEW,LIKE_NEW,USED_EXCELLENT` |

### Шаг 3: Запустите workflow

**Автоматический запуск:**
- Workflow автоматически запустится каждые 30 минут
- Первый запуск произойдет после пуша в main

**Ручной запуск (для тестирования):**

1. Перейдите в **Actions** → **eBay Tracker**
2. Нажмите **Run workflow** → **Run workflow**
3. Дождитесь завершения (30-60 секунд)
4. Проверьте логи

### Шаг 4: Проверьте работу

1. Откройте **Actions** в репозитории
2. Найдите запуск **eBay Tracker**
3. Нажмите на него и посмотрите логи:

```
🚀 eBay Tracker started
🔍 Keywords: joy division cassette
📍 Site: EBAY_US
💬 Telegram: enabled
──────────────────────────────────────────────────

🔎 Searching for: joy division cassette
   Found 12 items
   ✨ NEW: Joy Division - Unknown Pleasures...
   ...
✅ Scan complete: 5 new items total
```

4. Если настроен Telegram - проверьте уведомления на телефоне

## 🔧 Настройка расписания

Workflow настроен на запуск **каждые 30 минут**. Чтобы изменить:

Откройте `.github/workflows/ebay-tracker.yml`:

```yaml
on:
  schedule:
    # Каждые 30 минут (текущее)
    - cron: '*/30 * * * *'

    # Каждые 15 минут
    # - cron: '*/15 * * * *'

    # Каждый час
    # - cron: '0 * * * *'

    # Каждые 6 часов
    # - cron: '0 */6 * * *'

    # Два раза в день (9:00 и 21:00 UTC)
    # - cron: '0 9,21 * * *'
```

**Примечание:** GitHub Actions может задерживать запуск на 3-10 минут в час пик.

## 💾 Как работает база данных

Workflow использует **два способа** сохранения базы данных:

### 1. Artifacts (основной способ)
- База сохраняется как artifact после каждого запуска
- Восстанавливается перед следующим запуском
- Хранится 90 дней
- Не увеличивает размер репозитория

### 2. Git commit (резервный способ)
- База коммитится в репозиторий
- Позволяет видеть историю изменений
- Можно скачать и посмотреть локально

Чтобы **отключить коммит** базы (оставить только artifacts):

Удалите секцию в `.github/workflows/ebay-tracker.yml`:

```yaml
      - name: Commit database changes (alternative persistence)
        # Удалите или закомментируйте этот шаг
```

## 📊 Мониторинг и логи

### Просмотр логов

1. **Actions** → **eBay Tracker** → выберите запуск
2. Раскройте **Run eBay Tracker**
3. Увидите полный вывод скрипта

### Скачать базу данных

**Через Artifacts:**
1. **Actions** → выберите запуск
2. Внизу страницы найдите **Artifacts**
3. Скачайте `tracker-database`
4. Распакуйте и откройте в SQLite

**Через Git:**
```bash
git pull
# База будет в ebay-tracker/db/tracker.db
```

### Статистика

Добавьте шаг в workflow для вывода статистики:

```yaml
      - name: Show statistics
        run: |
          cd ebay-tracker
          python -c "
          from database import Database
          from config import Config
          db = Database(Config.DB_PATH)
          stats = db.get_stats()
          print('📊 Total items:', stats['total_items'])
          print('📊 Items today:', stats['items_today'])
          print('📊 By keyword:', stats['items_by_keyword'])
          "
```

## 🐛 Решение проблем

### Workflow не запускается

**Причина:** GitHub Actions отключены для форков или приватных репозиториев

**Решение:**
1. **Settings** → **Actions** → **General**
2. **Actions permissions:** выберите "Allow all actions"
3. Сохраните

### "Configuration errors: EBAY_APP_ID is required"

**Решение:** Добавьте секрет `EBAY_APP_ID`:
1. **Settings** → **Secrets** → **Actions**
2. **New repository secret**
3. Имя: `EBAY_APP_ID`, значение: ваш App ID

### База данных не сохраняется

**Решение 1 - проверьте artifacts:**
- **Actions** → выберите запуск
- Внизу должен быть `tracker-database` в Artifacts
- Если нет - проверьте логи на ошибки

**Решение 2 - используйте git commit:**
- Убедитесь что секция "Commit database changes" включена
- Дайте workflow права на запись:
  - **Settings** → **Actions** → **General**
  - **Workflow permissions:** "Read and write permissions"

### Telegram не отправляет сообщения

**Решение:**
1. Проверьте что добавили оба секрета: `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
2. Проверьте что вы написали боту хотя бы одно сообщение
3. Проверьте логи - должно быть "💬 Telegram: enabled"

### Workflow останавливается с ошибкой

**Решение:**
1. Откройте логи запуска в **Actions**
2. Найдите красную ошибку
3. Типичные проблемы:
   - **401 Unauthorized:** неверный `EBAY_APP_ID` или `EBAY_CERT_ID`
   - **Module not found:** проблема с зависимостями (обычно не бывает)
   - **Database locked:** две копии workflow запущены одновременно

## 💡 Советы

### Уменьшение количества запусков

Если 2000 минут не хватает:

1. **Увеличьте интервал:** с 30 минут до 1 часа
2. **Ограничьте время работы:**

```yaml
on:
  schedule:
    # Только в рабочие часы (9:00-21:00 UTC)
    - cron: '0 9-21/2 * * *'

    # Только по будням
    - cron: '0 */2 * * 1-5'
```

### Мониторинг нескольких поисков

Создайте несколько workflow с разными keywords:

```yaml
# .github/workflows/ebay-tracker-cassettes.yml
env:
  SEARCH_KEYWORDS: "joy division cassette,depeche mode cassette"

# .github/workflows/ebay-tracker-vinyl.yml
env:
  SEARCH_KEYWORDS: "joy division vinyl,depeche mode vinyl"
```

### Оповещения об ошибках

Добавьте шаг для отправки ошибок в Telegram:

```yaml
      - name: Notify on failure
        if: failure()
        run: |
          cd ebay-tracker
          python -c "
          from notifier import notify_error
          notify_error('Workflow failed - check logs on GitHub')
          "
```

## 📈 Расход минут

Примерный расчет для **приватного** репозитория:

- **1 запуск** ≈ 0.5-1 минута
- **Каждые 30 минут** = 48 запусков/день
- **48 запусков** × 30 дней = 1440 запусков/месяц
- **1440 запусков** × 1 минута = **1440 минут/месяц**

✅ Укладывается в бесплатный лимит 2000 минут!

Для **публичного** репозитория - **неограниченно бесплатно**.

## 🔐 Безопасность

### Приватный vs публичный репозиторий

**Приватный (рекомендуется):**
- Ваши ключевые слова не видны другим
- 2000 минут/месяц

**Публичный:**
- Неограниченные минуты
- Но ваши keywords видны всем
- Секреты остаются скрытыми

### Защита секретов

- Секреты зашифрованы GitHub и никогда не показываются в логах
- Используйте **secrets**, не .env файлы в репозитории
- Не добавляйте `.env` в git

## 📚 Дополнительные ресурсы

- [GitHub Actions документация](https://docs.github.com/en/actions)
- [Cron синтаксис](https://crontab.guru/)
- [eBay API документация](https://developer.ebay.com/api-docs)

## 🆘 Помощь

Если что-то не работает:

1. Проверьте **Actions** → логи запуска
2. Убедитесь что все секреты добавлены
3. Попробуйте запустить вручную через **Run workflow**
4. Проверьте что workflow не отключен (Settings → Actions)

---

**Готово!** Теперь ваш eBay Tracker работает в облаке 24/7 🎉
