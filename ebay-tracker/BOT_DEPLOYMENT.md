# 🤖 Деплой Telegram бота в облако

Инструкция по запуску bot.py в облаке 24/7 **бесплатно**.

## ☁️ Вариант 1: Render.com (рекомендуется)

**Бесплатный Background Worker** - идеально для Telegram ботов.

### Шаг 1: Подготовка

1. Убедитесь что код запушен на GitHub
2. У вас есть аккаунт на [render.com](https://render.com)

### Шаг 2: Создание Background Worker

1. Откройте [render.com/dashboard](https://dashboard.render.com)
2. Нажмите **New +** → **Background Worker**
3. Подключите GitHub репозиторий

### Шаг 3: Настройка

**Build settings:**

| Параметр | Значение |
|----------|----------|
| Name | `ebay-tracker-bot` |
| Region | `Frankfurt` (или ближайший) |
| Branch | `main` |
| Root Directory | `ebay-tracker` |
| Runtime | `Python 3` |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `python bot.py` |

### Шаг 4: Environment Variables

Добавьте секреты (в разделе **Environment**):

| Ключ | Значение | Обязательно |
|------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Токен от @BotFather | ✅ Да |
| `SEARCH_KEYWORDS` | Ваши ключевые слова | ✅ Да |
| `EBAY_APP_ID` | eBay App ID | ❌ Опционально* |
| `EBAY_SITE_ID` | `EBAY_US` | ❌ Опционально |

*Технически не нужен для бота, но можно добавить для команды /stats

### Шаг 5: Деплой

1. Нажмите **Create Background Worker**
2. Дождитесь завершения деплоя (~2 минуты)
3. Проверьте логи - должно быть:
   ```
   🤖 Starting eBay Tracker Bot...
   ✅ Bot started! Users can now subscribe with /start
   ```

### Шаг 6: Тест

1. Откройте Telegram
2. Найдите своего бота
3. Отправьте `/start`
4. Должно прийти приветственное сообщение

**Готово!** Бот работает 24/7 в облаке бесплатно.

---

## ⚡ Вариант 2: Railway.app

Ещё один бесплатный вариант.

### Быстрая установка:

1. Откройте [railway.app](https://railway.app)
2. **New Project** → **Deploy from GitHub repo**
3. Выберите репозиторий
4. **Add variables:**
   - `TELEGRAM_BOT_TOKEN`
   - `SEARCH_KEYWORDS`
5. **Settings** → **Start Command:** `python ebay-tracker/bot.py`
6. Deploy!

---

## 🐳 Вариант 3: Fly.io

Для продвинутых пользователей.

### Установка:

```bash
# Установите flyctl
curl -L https://fly.io/install.sh | sh

# Войдите
fly auth login

# Создайте app
cd ebay-tracker
fly launch --no-deploy

# Добавьте секреты
fly secrets set TELEGRAM_BOT_TOKEN="your_token"
fly secrets set SEARCH_KEYWORDS="joy division cassette"

# Деплой
fly deploy
```

---

## 📊 Сравнение платформ

| Платформа | Бесплатный лимит | Сложность | Автостарт |
|-----------|------------------|-----------|-----------|
| **Render.com** | 750 часов/мес | ⭐ Легко | ✅ Да |
| **Railway.app** | $5 кредита/мес | ⭐⭐ Средне | ✅ Да |
| **Fly.io** | 3 VMs бесплатно | ⭐⭐⭐ Сложно | ✅ Да |
| **PythonAnywhere** | Ограничено | ⭐ Легко | ❌ Нет |

**Рекомендация:** Используйте **Render.com** - проще всего настроить.

---

## 🔄 Совместная работа: Tracker + Bot

### Локально:

**Терминал 1:**
```bash
python bot.py  # Слушает подписки
```

**Терминал 2:**
```bash
python tracker.py  # Ищет лоты и отправляет подписчикам
```

### В облаке:

**Render.com - Background Worker:**
```
ebay-tracker-bot → bot.py (постоянно работает)
```

**GitHub Actions - Scheduled:**
```
tracker.yml → tracker.py (каждые 30 минут)
```

**База данных:**
- Оба используют `db/tracker.db`
- В GitHub Actions база сохраняется через artifacts + git
- Render.com имеет доступ к той же базе через git

---

## ⚠️ Важные моменты

### База данных на Render.com

**Проблема:** Background Worker на Render может перезапускаться, теряя локальные файлы.

**Решение 1:** Используйте Render Disk (платно $1/мес за 1GB)

**Решение 2:** Используйте внешнюю БД (PostgreSQL на Render бесплатно 90 дней)

**Решение 3 (простое):** Оставьте bot.py на Render, а tracker.py на GitHub Actions - тогда база в GitHub

### Рекомендуемая архитектура:

```
┌─────────────────────────────────────────┐
│         GitHub Repository                │
│  ├── ebay-tracker/                      │
│  │   ├── bot.py                         │
│  │   ├── tracker.py                     │
│  │   └── db/tracker.db (synced via git) │
└─────────────────────────────────────────┘
           │              │
           │              │
    ┌──────▼──────┐  ┌───▼──────────┐
    │  Render.com │  │GitHub Actions│
    │   bot.py    │  │  tracker.py  │
    │  (24/7)     │  │ (every 30m)  │
    └─────────────┘  └──────────────┘
           │              │
           └──────┬───────┘
                  │
         ┌────────▼────────┐
         │  Telegram API   │
         │  (notifications)│
         └─────────────────┘
```

### Синхронизация базы:

**В workflow добавлено:**
```yaml
- name: Commit database changes
  run: |
    git add db/tracker.db
    git commit -m "Update database"
    git push
```

**На Render.com (если нужно):**

Добавьте в `bot.py` периодическую синхронизацию:

```python
# Раз в час делать git pull для актуальной базы
import subprocess
import schedule

def sync_db():
    subprocess.run(['git', 'pull', 'origin', 'main'])

schedule.every(1).hours.do(sync_db)
```

---

## 🧪 Тестирование

### Проверка деплоя:

1. **Логи на Render.com:**
   - Dashboard → ebay-tracker-bot → Logs
   - Должно быть: `✅ Bot started!`

2. **Telegram тест:**
   ```
   Вы: /start
   Бот: ✅ Добро пожаловать в eBay Tracker!

   Вы: /status
   Бот: ✅ Подписка активна

   Вы: /stats
   Бот: 📊 Статистика eBay Tracker
   ```

3. **Проверка уведомлений:**
   - Запустите tracker.py локально
   - Должно прийти уведомление в Telegram

---

## 🐛 Решение проблем

### "Application error" на Render

**Причина:** bot.py не может запуститься

**Решение:**
1. Проверьте логи в Render Dashboard
2. Убедитесь что все Environment Variables добавлены
3. Проверьте Start Command: `python bot.py`

### "Bot doesn't respond" в Telegram

**Причина:** Бот не запущен или неверный токен

**Решение:**
1. Проверьте логи - должно быть "Bot started"
2. Проверьте `TELEGRAM_BOT_TOKEN` корректен
3. Попробуйте перезапустить worker (Manual Deploy)

### "No subscribers" в логах tracker.py

**Причина:** База данных не синхронизирована

**Решение:**
1. Убедитесь что database.py создает таблицу subscribers
2. Проверьте что подписались через /start
3. Запустите локально для проверки

### База данных теряется на Render

**Решение:**

Добавьте Render Disk:

1. Dashboard → ebay-tracker-bot → Disk
2. Add Disk: `/opt/render/project/src/ebay-tracker/db`
3. Size: 1 GB ($1/мес)

Или используйте PostgreSQL вместо SQLite.

---

## 💰 Стоимость

### Полностью бесплатно:

- ✅ **Render.com** - 750 часов/мес (хватит на 1 worker 24/7)
- ✅ **GitHub Actions** - 2000 минут/мес
- ✅ **Telegram Bot API** - бесплатно

### Итого: **$0/месяц** для работы 24/7!

---

## 📚 Дополнительные ресурсы

- [Render Background Workers](https://render.com/docs/background-workers)
- [Railway Docs](https://docs.railway.app/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Готово!** Теперь ваш бот работает в облаке круглосуточно 🎉
